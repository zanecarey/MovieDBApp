<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zane.moviedbapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NowPlayingDisplay"></activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FilterResults"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".DisplayResults"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MovieDetails"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".WatchListDisplay"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MovieRatingsDisplay"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".BaseActivity" />
        <activity
            android:name=".ActorInfoActivity"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>